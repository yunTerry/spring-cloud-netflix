server:
  port: 3600
management:
  security:
    enabled: false
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yunTerry/spring-cloud-netflix
          search-paths: config-repo